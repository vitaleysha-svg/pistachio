# ============================================================
# PISTACHIO — Dataset Config (Training Data)
# Points to your TRAIN folder with captioned images
# ============================================================

resolutions = [512]

enable_ar_bucket = true
min_ar = 0.5
max_ar = 2.0
num_ar_buckets = 7

# Frame buckets: [1] = images only. Add [1, 33] for video+image mixed.
frame_buckets = [1]

[[directory]]
path = '/workspace/dataset/train'
num_repeats = 4
